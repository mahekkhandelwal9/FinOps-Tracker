{
  "name": "finops-platform",
  "version": "1.0.0",
  "description": "FinOps Platform - A comprehensive cloud cost management tool",
  "main": "index.js",
  "scripts": {
    "start": "cd backend && npm start",
    "install:all": "cd backend && npm install && cd ../frontend && npm install",
    "dev": "concurrently \"cd backend && npm start\" \"cd frontend && npm start\"",
    "build": "cd frontend && npm run build",
    "vercel-build": "cd frontend && npm install && npm run build && cd .. && node copy-build.js",
    "test": "echo \"No tests specified\" && exit 1"
  },
  "keywords": [
    "finops",
    "cloud-cost",
    "budget-management"
  ],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
